"""Algorithms used by plot pool figure generation.

Pure pandas/numpy reference implementations that document the exact steps
taken in FigureGenerator (e.g. group plot aggregation, pre-filtering).

Also includes interval statistics (intv_stats) for time-series event columns.
"""
