services:
  nicewidgets-demo:
    build:
      context: ../..
      dockerfile: examples/main_demo/Dockerfile
    image: nicewidgets-demo:latest
    ports:
      - "8080:8080"
    environment:
      PYTHONUNBUFFERED: "1"
      NICEWIDGETS_DEMO_HOST: "0.0.0.0"
      PORT: "8080"
